syntax = "proto3";

package analytics

option go_package = "megareds/mediahub/api/proto/analytics";

service AnalyticsService {
    rpc GetAnalytics(GetAnalyticsRequest) returns (GetAnalyticsResponse);
}

message GetAnalyticsRequest {
    string video_id = 1;
}

message GetAnalyticsResponse {
    string video_id = 1;
    int64 view_count = 2;
}